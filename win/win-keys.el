;;; The normal windows shortcut keys in emacs
;will add to these as I go along
;(global-set-key (kbd "C-<S-W>") 'kill-this-buffer) ; "C-w" is copy, so we've added shift
(global-set-key  (kbd "M-<f4>") 'save-buffers-kill-terminal) ; usually tied to C-x C-c
