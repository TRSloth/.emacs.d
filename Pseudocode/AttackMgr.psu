AttackMgr(possibleVictim, i = 0)
    attemptedAttack = [KnobAttack, BlurTooth, BLESA]
    exploitedConnection;
    Try(exploitedConnection = attemptedAttack[i](possibleVictim)):
        RelayData(DataReader(exploitedConnection))
    Catch(someError)
        log(someError)
    if TestInRange(possibleVictim)
        i = (i+1)% attemptedAttack.length
        AttackMgr(possibleVictim, i)
    return

